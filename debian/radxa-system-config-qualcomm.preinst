#!/bin/sh
set -e

case $1 in
	install | upgrade)
		conflicts="
			/usr/share/drirc.d/00-mesa-defaults.conf
			"
		for i in $conflicts; do
			dpkg-divert --package radxa-system-config-qualcomm --divert "$i.bak" --rename "$i"
		done
		;;
esac

#DEBHELPER#

exit 0
