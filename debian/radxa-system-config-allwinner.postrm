#!/bin/sh
set -e

#DEBHELPER#

case $1 in
    remove)
        conflicts="
        /usr/share/applications/mimeapps.list
        "
        for i in $conflicts
        do
            dpkg-divert --package radxa-system-config-allwinner --rename --remove "$i"
        done
        ;;
esac

exit 0