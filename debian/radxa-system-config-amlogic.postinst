#!/bin/sh
set -e

case $1 in
    configure)
        if [ ! -e /var/lib/sddm/state.conf ]
        then
            cp /usr/share/doc/radxa-system-config-amlogic/state.conf /var/lib/sddm/state.conf
        fi
        ;;
esac
    
#DEBHELPER#

exit 0