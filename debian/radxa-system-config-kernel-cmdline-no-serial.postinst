#!/bin/sh
set -e

case $1 in
    configure)
        if [ ! -f /etc/kernel/cmdline ]
        then
            install -m 644 /usr/share/radxa-system-config/cmdline /etc/kernel/cmdline
            u-boot-update
        fi
        ;;
esac
    
#DEBHELPER#

exit 0
